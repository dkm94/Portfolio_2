import React from "react";
import PropTypes from "prop-types";

const Container = ({ projets }) => {
    // On récupère les objets projets via props

    return(
        <div>

        </div>
    )
}

Container.propTypes = {
    projets: PropTypes.object
}

export default Container;